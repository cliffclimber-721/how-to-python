import addsub_mod
# We put " import " to use modules. ' addsub_mod ' is the code file using for ' import '.

print(addsub_mod.add(3, 4)) # This means I'll use ' add ' function at ' addsub_mod' module.
print(addsub_mod.sub(3, 4)) # Also this too.